server.port=${SERVER_PORT:8080}
spring.application.name=tms-backend
spring.profiles.active=${ACTIVE_PROFILE:dev}
app.base-url=http://localhost:8080
# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/tms_db_dev}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:projetpfe}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false
spring.sql.init.mode=never

# JWT Configuration
jwt.secret=${JWT_SECRET:VotreCleSecreteTresLongueEtSecurisee123456789GRPO2024TMS}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Mail Configuration
spring.mail.host=74.125.133.108
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=bounebtassnim217@gmail.com
spring.mail.password=dvqn qsie sssw pbpc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Email Application Configuration
app.email.from=${MAIL_FROM:TMS GRPO Consulting <bounebtassnim217@gmail.com>}
app.email.enabled=true
app.email.username=${APP_EMAIL_USERNAME:bounebtassnim217@gmail.com}

# Twilio Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID:}
twilio.auth.token=${TWILIO_AUTH_TOKEN:}
twilio.phone.number=${TWILIO_PHONE_NUMBER:}
twilio.enabled=${TWILIO_ENABLED:false}

# Task/Async Configuration
spring.task.scheduling.pool.size=5
spring.task.execution.pool.core-size=5

# Logging Configuration
logging.level.com.grpo.tms=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=WARN

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Africa/Tunis

# Other Configuration
spring.main.allow-bean-definition-overriding=true
spring.thymeleaf.check-template-location=false
logging.file.name=logs/application.log
logging.level.io.jsonwebtoken=DEBUG
server.servlet.context-path=/api

management.endpoint.mappings.enabled=true
spring.mvc.static-path-pattern=/static/**
spring.web.resources.add-mappings=false
management.endpoints.web.exposure.include=*
management.endpoints.web.base-path=/actuator